# Getting Started with React Native

React Native lets you build mobile apps using JavaScript and React. You can use it with or without frameworks like Expo.

## Why Use Expo?

- Simplifies setup  
- Cross-platform support  
- Built-in native modules  

## Create a New Project

To create a new project using Expo, run the following command in your terminal:

```bash
npx create-expo-app@latest

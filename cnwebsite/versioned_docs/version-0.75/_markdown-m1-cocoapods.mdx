<details>
    <summary>Mac M1 的注意事项</summary>

Cocoapods 目前在 Mac M1 架构上可能还有一些兼容问题（我们建议使用`brew install cocoapods`来安装 Cocoapods）。如果你在安装 pods 依赖时出现问题，可以尝试运行下面的命令： 

- `sudo arch -x86_64 gem install ffi`
- `arch -x86_64 pod install`

以上命令会安装`ffi`包，用于在安装和装载 pods 时调用合适的系统架构。

</details>
